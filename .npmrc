# NPM Configuration
# Ensure consistent package installation across environments

# Use exact versions for production stability
save-exact=true

# Disable package-lock.json updates during install
package-lock=true

# Audit level for security vulnerabilities
audit-level=moderate
